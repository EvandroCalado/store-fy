import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Administração',
};

const AdminPage = () => {
  return (
    <div>
      <h1>AdminPage</h1>
    </div>
  );
};

export default AdminPage;
