import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Produtos',
};

const ProductsPage = () => {
  return (
    <div>
      <h1>ProductsPage</h1>
    </div>
  );
};

export default ProductsPage;
